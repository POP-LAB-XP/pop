<div class="panel panel-warning">
  <div class="panel-heading">
    <h3 class="panel-title">Veto criado para a proposta número <%= @veto.proposta_id%></h3>
  </div>
  <div class="panel-body">
  	<div class="row">
  		<div class="col-md-6">
    		<h3>Proposta:</h3>
    		<strong> <%= @veto.proposta_descricao %></strong>
    	</div>
  		<div class="col-md-6">
    		<h3>Motivo do veto:</h3>
    		<strong> <%= @veto.descricao %></strong>
    		<p><strong>Criado em <%= @veto.created_at %></strong></p>
    	</div>
  	</div>
  </div>
</div>

<div class="col-sm-offset-4 col-sm-4">
	<%= link_to("Voltar",propostas_path,:class => 'btn btn-default -block btn') %>

</div>

